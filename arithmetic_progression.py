def find_n_term(a1, d, n):
    # Формула n-го члена арифметической прогрессии: a_n = a_1 + (n-1)*d
    return a1 + (n - 1) * d

# Пример: первый член = 5, разность = 2, ищем 10-й член
a1, d, n = 5, 2, 10
result = find_n_term(a1, d, n)

print(f"10-й член прогрессии: {result}")
